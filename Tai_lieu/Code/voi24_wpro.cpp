#include <bits/stdc++.h>
using namespace std;
#define int long long
int n,l1, l2, c, d;

void sub1()
{
    int sum = 0, ans = 0;
    for (int i = 1; i <= n; i++)
    {
        int need = l1 - sum;

    }
}
signed main()
{
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    cin >> n >> l1 >> l2 >> c >> d;
    for (int i = 1; i <= n; i++) cin >> a[i];
    if (l1 == l2) sub1();

}
