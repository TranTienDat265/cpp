//#include <bits/stdc++.h>
#include <iostream>
#include <math.h>
using namespace std;
//ifstream fi ("input.txt");
//ofstream fo ("output.txt");
int main()
{
    /*  STRING
    string s1,s2,st;
    //while (!fi.eof()){
    //    getline(fi,s);
    //    fo << s << endl;
    getline(fi,s1);
    getline(fi,s2);
    //Các phép toán
    st=s1+s2; //Nối xâu
    if (st==s1+s2) //So sánh xâu
        fo << "True\n";
    else
        fo << "False\n";
    //Các hàm xử lí xâu :
    //1. s.length() Trả về độ dài của chuổi s
    fo << s1.length() << " " << s2.length() << endl;
    //2. s.substr(x,y) Rút trích một chuỗi con với chiều dài y bắt đầu tại vị trí x. Nếu không có y, một chuỗi con từ vị trí x tới cuối chuỗi sẽ được rút trích.
    st=s2.substr(0,6);
    fo << st << endl;
    //3. s.find(r) Kiểm tra chuỗi string r có xuất hiện trong chuỗi s hay không. Nếu có thì trả về vị trí bắt đầu xuất hiện chuỗi r trong chuỗi s.
    fo << s2.find("Tan") << endl;
    //4. s.erase(x, n) Xóa n ký tự bắt đầu tại vị trí x.
    s2.erase(0,3);
    fo << s2 << endl;
    //5. s.replace(x, n, str) Thay thế n ký tự tại vị trí bắt đầu là x bằng chuỗi str. Lưu ý: chiều dài của str có thể lớn hơn n.
    s2.replace(0,1,"TanD");
    fo << s2 << endl;
    //6.Hàm s1.insert(index, s2) Thêm chuỗi s2 vào s1 sau vị trí index.
    s2.insert(s2.length()," DDT");
    fo << s2 << endl;
    */

    //KIỂU DỮ LIỆU, NHẬP XUẤT
    int n; //Khai bái biến vs kiểu dữ liệu Interger
    long long m; // long long tương đương vs longint;
    float a = 1.1111;//Số thực
    double b;//Số thực với độ chính xác cao hơn
    cout << "Nhap N:"; //In ra màn hình "Nhap N:"
    cin >> n; //Nhập n từ bàn phím
    cout << n << endl; //In giá trị của n ra màn hình và xuống dòng
    cout << fixed << setprecision(2) << a <<endl;// Lấy 2 chữ số thập phân


    //
    return 1;
}

