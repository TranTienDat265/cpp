#include <bits/stdc++.h>
using namespace std;
#define int long long 

signed main()
{
	ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
	int n, l, r; cin >> l >> r;
	for (int i = 1; i <= n; i++)
		cin >> w[i] >> v[i];
	int mid = n / 2;

	int MASK1 = (1 << mid);
	for (int mask = 0; mask <= MASK1; mask++)
	{
		for (int i = 0; i < mid; i++)
			if 
	}
}